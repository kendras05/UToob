<h1>Admin Test</h1> 

<div id=“boxes”>
<h1>List of Users</h1>

<% @users.each do |user| %>
   <%= user.email %>
 <%= link_to(“user comments”, comments_user_path(user.id)) %>
-
		<%= link_to(“edit”, edit_user_path(user.id)) %>
		-
		<%= link_to(“delete”, user_path(user.id), method: :delete, data: {confirm: Are you sure you want to delete this user?}) %>


<% end %>
</div>

<div id=“boxes”>
<h1> List of Trailers </h1>

<% @movies.each do |movies| %>
	<p>
		<%= move.title %>
		-
		<%= link_to(“edit”, edit_movie_path(movie.id)) %>
		-
		<%= link_to(“delete”, movie_path(movie.id), method: :delete, data: {confirm: Are you sure you want to delete this video?}) %>
      </p>
<% end %>

</div>


